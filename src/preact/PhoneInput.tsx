import { forwardRef } from 'preact/compat';

export const PhoneInput = forwardRef(function PhoneInput(
  _props: Record<string, unknown>,
  _ref,
) {
  // TODO: implement — thin wrapper around vanilla widget
  return null;
});
